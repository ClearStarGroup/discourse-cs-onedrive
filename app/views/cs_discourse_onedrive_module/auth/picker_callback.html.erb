<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>OneDrive Picker</title>
    <script src="https://js.live.net/v8.0/OneDrive.js" nonce="<%= csp_nonce_placeholder %>"></script>
  </head>
  <body>
    <script nonce="<%= csp_nonce_placeholder %>">
    // This page is used as a callback for the OneDrive picker
    // The picker SDK will handle the redirect and communicate back to the parent window
    console.log("OneDrive picker callback page loaded");
    
    // The OneDrive picker SDK should automatically handle the callback
    // and communicate with the parent window via postMessage
    // If we're in a popup, the picker will close it automatically when done
    
    // v8 picker works with single-tenant apps when provided with an accessToken
    // that was acquired from the tenant-specific endpoint (via MSAL)
    </script>
  </body>
</html>

